from distutils.core import setup
setup(name='ezutil',
      version='0.0.1',
      packages=['ezutil'],
      description = "tools collection to make x7 dev easier",
      author = "whoami",
      )
